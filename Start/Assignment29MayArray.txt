1.	 int ,long - 0
	 float ,double - 0.0
	 boolean - false
	 String - null

2.No,we cannot pass negative numbers in array size and compile time error.

3.Array is stored in heap memory.

4.The limitations of array is fixed size.we cannot increase or decrease the size.
  It requires contiguous memory.

5.An array wiithout any name is called anonymous array.

6.using for loop and for each loop.

7.length is used in arrays concept,it returns the size of the array.length is not a method.
  length() is used in String concept,it returns the size of the string.length() is a static method in string class
  